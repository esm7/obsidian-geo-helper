import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.esm.geohelper',
  appName: 'obsidian-geo-helper',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
